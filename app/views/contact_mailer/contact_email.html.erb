

<!DOCTYPE html>
<html>
  <head></head>
    <body>
      <p>You have recieved a message from the site's Contact form, From
        <%= "#{@name}, #{@email}." %></p>
        <p><%= "#{@body}." %></p>
    </body>
  </html>
